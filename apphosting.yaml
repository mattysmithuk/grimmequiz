# Firebase App Hosting configuration.
# https://firebase.google.com/docs/app-hosting/configure
#
# This makes the variables the app needs available at runtime.
# Secrets are pulled from Cloud Secret Manager and never committed.

runConfig:
  minInstances: 0
  maxInstances: 2
  concurrency: 80
  cpu: 1
  memoryMiB: 512

env:
  # Admin leaderboard password. Stored in Cloud Secret Manager, injected at runtime.
  # Create it once with:
  #   firebase apphosting:secrets:set ADMIN_PASSWORD
  #   firebase apphosting:secrets:grantaccess ADMIN_PASSWORD --backend <backend-id>
  - variable: ADMIN_PASSWORD
    secret: ADMIN_PASSWORD
    availability:
      - RUNTIME
